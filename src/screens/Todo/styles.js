import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textButon: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0
  },
  view: {
    flex: 1
  }
});

export default styles;
