import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  commentsContainer: {
    borderColor: '#ddd',
    borderWidth: 1,
    margin: 5,
    padding: 10,
    elevation: 2
  },
  commentText: {
    paddingBottom: 5,
    fontSize: 25,
    fontWeight: 'bold'
  }
});

export default styles;
